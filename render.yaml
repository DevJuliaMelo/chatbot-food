services:
  - type: web
    name: chatbot-ifood
    env: python
    rootDir: chatbot-ifood
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false
